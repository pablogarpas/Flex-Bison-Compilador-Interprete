programa prueba2

const
	var1 "comparando"

var
	aux entero
	aux3 entero

inicio
/* asd asd  */

	escribir("programa")
	escribir(aux)
	llamar(prueba)

	aux=5

	escribir(aux)
fin

funcion prueba
arg
	aux2 entero
	a real 
	aux string
var
	i entero
devolver bool

escribir("funciona")
fin


funcion qwer
arg
	aux2 entero
	a real 
	aux string
var
	i entero
devolver bool

escribir("no funciona")
fin
