programa prueba

var
	aux2 string
	aux entero
inicio
	aux2= "qwe"
	aux=15
	
	escribir(aux2)
	llamar(probando,a,b,c)
	
	escribir(aux2)
fin

funcion probando
var
	aux entero
arg
	a entero
	b string
	c string

devolver bool

escribir(aux2)
escribir("qwe")
escribir(b)

fin
