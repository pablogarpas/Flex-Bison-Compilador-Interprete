programa prueba2

var
	aux entero
	aux3 string
inicio
	aux=15
	aux3="holi"
	
	aux=llamar(prueba,aux,aux3)
	
	escribir(aux)
	
fin

funcion prueba
arg
	aux2 entero
	aux4 string
devolver entero

	escribir(aux2)
	escribir(aux4)
	
	aux2=16
	
	devolver aux2
fin
