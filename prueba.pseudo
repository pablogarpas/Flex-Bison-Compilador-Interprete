programa prueba

var
	aux real
	aux2 real
	a entero
inicio
	aux=5
	a=6
	escribir(aux)
	llamar(probando)
	
	aux2= 5.21
	escribir(aux2)
fin

funcion probando
arg
	aux2 real
	a string

devolver bool
	aux2=7
	a="funcion"
escribir(a)
escribir(aux2)
fin
