programa prueba

var
	aux real
	aux2 real
	a entero
inicio
	aux=5
	a=6
	escribir(aux)
	llamar(qwer)
	
	aux2= 5.21
	escribir(aux2)
fin

funcion qwer
arg
	aux2 real
	a string

devolver bool
	aux2=7
	a="qwe"
escribir(a)
escribir(aux2)
fin
