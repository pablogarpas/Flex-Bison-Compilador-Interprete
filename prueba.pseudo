programa prueba2

librerias
	"stdio.h"
	"math.h"

const
	asd "comparando"

var
	aux entero 
	aux3 entero

inicio
$$ asd $$

	aux=2

	escribir("hola")
	escribir("adios")
	
	llamar prueba qwe


si *aux==2 hacer
	escribir("dsa")
	escribir("asd")
	aux2=aux
fin

fin

funcion prueba
arg
	aux2 entero
	a real 
	aux string
var
	i entero
devolver bool

escribir("asd")
escribir("dsa")
fin
