programa prueba2

var
	aux entero
	aux3 string
inicio
	aux=15
	aux3="holi"
	
	aux=llamar(qwe)
	
	escribir(aux)
	
fin

funcion prueba
var
	aux6 string
arg
	aux2 entero
	aux4 string
devolver entero

	escribir(aux2)
	escribir(aux4)
	
	aux2=18
	
	llamar(qwe)
	
	devolver aux2
fin


funcion qwe
devolver entero

escribir("asd")
	devolver 21

fin
