programa prueba2

const
	asd "comparando"

var
	aux entero
	aux3 entero

inicio
$$ asd $$

	escribir("programa")
	llamar(prueba)

fin

funcion prueba
arg
	aux2 entero
	a real 
	aux string
var
	i entero
devolver bool

escribir("funciona")
fin


funcion qwer
arg
	aux2 entero
	a real 
	aux string
var
	i entero
devolver bool

escribir("no funciona")
fin
